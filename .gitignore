node_modules
dist
public/geo
.DS_Store
*.local
public/flags/
.cache/

# Local preview-server config, not part of the project
.claude/

# EuroGlobalMap source download (about 1 GB): the build reads it, and it is never committed.
/euro-global-map-GPKG/
